# disect
Disect is a command line tool to find the dependencies that caused a regression in a python environment by leveraging combinatorical group testing to minimize the number of environments to test.
